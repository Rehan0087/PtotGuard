# The Next.js app proxies /api requests to the NestJS server on :3001.
# This is the persistent, Postgres-backed default; use `enabled` only for
# isolated UI work against the MSW fixtures.
NEXT_PUBLIC_API_MOCKING=disabled
