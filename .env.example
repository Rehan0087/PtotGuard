# `pnpm dev` is a standalone frontend demo and uses MSW fixtures. Set this to
# `disabled` only when the NestJS API is also running (or use `./demo.sh start`,
# which selects the persistent Postgres-backed stack automatically).
NEXT_PUBLIC_API_MOCKING=enabled
